<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bjsasc.plm.mapper.BaseLineLinkMapper">
    <insert id="insert" parameterType="BaseLineLink">
        INSERT INTO baseline_link (baseline_id, doc_id, version_no) values (#{baseLineId}, #{docId}, #{versionNo})
    </insert>
</mapper>
